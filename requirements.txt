# ── Web / HTTP ──────────────────────────────
fastapi
uvicorn[standard]
python-dotenv
httpx
anthropic

# ── Database & ORM ─────────────────────────
sqlalchemy>=1.4
asyncpg                        # async Postgres driver
alembic
psycopg2-binary               # if you ever need sync DB access

# ── Auth, Security & Tokens ────────────────
passlib[bcrypt]
python-jose[cryptography]
email-validator                # for Pydantic’s EmailStr

# ── (Optional) Form parsing for OAuth2) ────
python-multipart
